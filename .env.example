# Database
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# For local development: postgresql://postgres:postgres@localhost:5432/next_starter
# For cloud (e.g., Neon, Supabase): Get connection string from your provider
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/next_starter?schema=public"

# Auth.js v5 Configuration
# Generate a secret with: openssl rand -base64 32
# Or online: https://generate-secret.vercel.app/32
AUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Application URL
# Change this to your production URL when deploying
NEXTAUTH_URL="http://localhost:3000"

# WebAuthn Configuration (Passkeys)
# For local development, use localhost. For production, use your domain.
# Example production: https://yourdomain.com
RP_ID="localhost"
RP_NAME="Next.js Starter with Passkeys"
RP_ORIGIN="http://localhost:3000"
